CREATE TABLE quiz_flash_sets (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    quiz_id INTEGER REFERENCES quiz_me_quizzes(id) ON DELETE CASCADE NOT NULL,
    flash_id INTEGER REFERENCES quiz_me_flashcards(id) ON DELETE CASCADE NOT NULL
);